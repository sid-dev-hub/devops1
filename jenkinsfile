pipeline {
    agent {label 'nginx'}

    stages{
/*      	stage("Checkout"){
        	steps{
	  		git branch: 'dev', poll:true, url: 'https://github.com/sid-dev-hub/devops1'
	}
      } */

   	stage("Build"){
      		steps{
	  		sh "cat /etc/os-release"
			
	}
      } 
   }
}
