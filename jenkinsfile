pipeline {
    agent {
		docker {
			image 'sidsalakki14/demodocker'
			
		}
	}

    stages{
/*      	stage("Checkout"){
        	steps{
	  		git branch: 'dev', poll:true, url: 'https://github.com/sid-dev-hub/devops1'
	}
      } */

   	stage("Build"){
      		steps{
	  		sh "docker build -t nginx-img ."
			sh "docker run -d -p 7000:80 nginx-img"
	}
      } 
   }
}
